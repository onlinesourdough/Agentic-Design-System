interface:
  display_name: "Agentic Design System"
  short_description: "Create portable visual directions and handoffs"
  default_prompt: "Use $agentic-design-system to inspect prior runs, make DESIGN.md the canonical portable visual direction, review it and any selected preview, create an explicit cross-owner handoff when needed, record proof, and promote only deliberate examples."
