# Third-party notices

The repository depends on `@google/design.md@0.3.0` and Prettier `3.6.2`
through the pinned npm lockfile. Their licenses are installed with the packages
and remain governed by their upstream terms.

The UI sources listed in [`SOURCE_AUDIT.md`](SOURCE_AUDIT.md) are reviewed
references with their license signals recorded per source. ADS does not copy
or redistribute their component catalogs, source bundles, brand assets, or
skill catalogs. The one Resources adapter is original ADS-owned CSS and has no
external package dependency.

OpenPencil is an optional reviewed tool adapter, not an npm dependency or
vendored library. The active `.op` file and PNG contain ADS-owned design
content. Release `v0.8.4`, its MIT license signal, exact revisions, temporary
binary verification, reuse boundary, and fit are recorded in
[`SOURCE_AUDIT.md`](SOURCE_AUDIT.md); no OpenPencil executable or upstream
asset is committed.

The carried Resources preview includes local Geist fonts and a bundled
`assets/fonts/LICENSE.txt`; that notice remains next to the assets. Media
provenance and the user-owned carry proof are recorded in the example asset
README and [`preservation.md`](preservation.md).
